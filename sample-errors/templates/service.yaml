apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-errors.fullname" . }}
  labels:
    {{- include "sample-errors.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-errors.selectorLabels" . | nindent 4 }}
